assignment.txt
.env
*.json.key

# Python
__pycache__/
*.pyc
.venv/
.coverage
.pytest_cache/

# Node
node_modules/
dist/
.vite/

# Data (raw + processed are large, reproducible from fetch scripts)
archive/app1-zone-map/data/raw/
archive/app1-zone-map/data/processed/

# ITM explorer (documentation/explore.py): 454 raw PDFs (~320 MB) + pipeline
# caches, all reproducible via `make explore`. report.html stays committed.
documentation/data/

# App2 generated artifacts: extraction, pair caches, merged analysis, usage
# ledger, resolutions. All reproducible via `make analyze`.
app2-conflict-resolver/backend/data/
documentation/dist/
