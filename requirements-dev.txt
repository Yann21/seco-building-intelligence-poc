# Dev tooling for the SECO PoC (not needed at runtime / in the Docker image).
#   pip install -r requirements-dev.txt
ruff>=0.15           # lint + format (black / isort / flake8 / pyupgrade, consolidated)
pytest>=8.0          # test runner
pytest-cov>=5.0      # coverage plugin
coverage>=7.0        # coverage measurement (json report for the doc server)
httpx>=0.27          # required by fastapi.testclient
